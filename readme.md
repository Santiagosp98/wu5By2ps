# MachEight technical assessment

## Installation

1. Install PHP(8.0). 
2. Install [Composer](https://getcomposer.org/).
3. Run `composer install` for required dependencies.

## Run

From the CLI, run the script by typing:

`php index.php`

You will be prompted to enter a number:

`Enter the number:`
